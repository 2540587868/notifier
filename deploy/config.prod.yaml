server:
  listen: ":8081"
  admin_token: "CHANGE_ME_IN_PRODUCTION"

database:
  path: "/opt/notifier/notifier/data/notifier.db"

default_channels:
  - "email-admin"

rate_limit:
  per_source_level:
    window: "5m"
    max: 10
  per_source:
    window: "5m"
    max: 50
  global:
    window: "1m"
    max: 100

retry:
  max_attempts: 3
  base_delay: "2s"
  max_delay: "30s"

channels:
  - name: "email-admin"
    type: "email"
    config:
      smtp_host: "smtp.qq.com"
      smtp_port: "465"
      username: "2540587868@qq.com"
      password: "gxgwbuwmghtndiaj"
      from: "2540587868@qq.com"
      to: "2540587868@qq.com"
    filter:
      levels: ["critical", "error", "warning", "info"]
    enabled: true

  - name: "napcat-qq"
    type: "napcat"
    config:
      api_url: "http://127.0.0.1:3000"
      access_token: ""
      user_id: "2540587868"
      message_type: "private"
    filter:
      levels: ["critical", "error"]
    enabled: false

silence:
  windows: []
